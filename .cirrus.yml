# poke again again

task:
  name: Hack
  compute_engine_instance:
    image_project: pg-vm-images-aio
    image: family/pg-aio-freebsd-13-0
    platform: freebsd
    cpu: 4
    memory: 8G
  hello_script:
    - echo '===mount==='
    - mount
    - echo '===gpart==='
    - gpart list
    - echo '===fstab==='
    - cat /etc/fstab
    - echo '===df==='
    - df -h
    - echo '===new partition==='
    - sudo gpart add -t freebsd-ufs -l data8k da0
    - echo '===gpart==='
    - gpart list
