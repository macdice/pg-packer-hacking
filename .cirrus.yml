# poke again

task:
  name: Hack
  compute_engine_instance:
    image_project: pg-vm-images-aio
    image: family/pg-aio-freebsd-13-0
    platform: freebsd
    cpu: 4
    memory: 8G
  hello_script:
    - echo '===mount==='
    - mount
    - echo '===gpart==='
    - gpart list
    - echo '===fstab==='
    - cat /etc/fstab
